box: python:3.6
build:
  steps:
    # Install package
    - script:
      name: install package
      code: pip install .[dev,plotly,server]

    # Run formatters
    - script:
      name: run formatters
      code: |
        isort --check-only
        black --check .

    # Run linters
    - script:
      name: run linters
      code: flake8

    # Run tests
    - script:
      name: run unit tests
      code: pytest tests/unit

