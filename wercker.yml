box: python:3.6
build:
  steps:
    # Install package
    - script:
      name: install package
      code: |
        pip install poetry
        poetry install -E server

    # Run linters
    - script:
      name: run linters
      code: make lint

    # Run tests
    - script:
      name: run unit tests
      code: make test